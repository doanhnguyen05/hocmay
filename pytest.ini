[pytest]
pythonpath = .
testpaths = tests
norecursedirs = artifacts __pycache__ .git .pytest_cache
markers =
    slow: tests that run model training or scan many URLs
    network: tests that require external network access
